const express = require("express"); const app = express(); app.get("/health", (req, res) => { res.json({ status: "ok", service: "booking-service" }); }); app.listen(3003, () => { console.log("Booking Service running on port 3003"); });
